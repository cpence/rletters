.modal-dialog{ role: 'document' }
  .modal-content
    .modal-header
      %h4.modal-title= t('.header')
      = close_icon(dismiss: 'modal')

    = render partial: 'form', locals: { category: @category, url: categories_path, method: :post }
