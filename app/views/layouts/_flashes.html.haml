- flash.each do |key, value|
  %div{ class: "alert alert-dismissible alert-#{ { success: 'success', alert: 'danger' }[key.to_sym] || 'info' }" }
    %button.close{ type: 'button', data: { dismiss: 'alert' } }
      %span{ "aria-hidden" => "true" }×
      %span.sr-only Close
    = value
