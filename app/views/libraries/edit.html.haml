%h4 Edit your library details:
%h6.subheader Fill in the details to your local library's OpenURL resolver, and we'll take it from there.

= render partial: 'form', locals: { url: library_path(@library) }
%a.close-reveal-modal &#215;
