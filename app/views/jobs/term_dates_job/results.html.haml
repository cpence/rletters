- data = JSON.load(task.json)

.row
  .col-md-8
    %h1== #{Dataset.model_name.human}: #{dataset.name}
    %h4.text-muted= t('term_dates_job.subheader', term: data['term'])

  .col-md-4.job-header-button
    = link_to t('common.download_csv'), task.download_path_for('text/csv'), class: 'btn btn-default'

.row
  .col-xs-12
    %hr

    %script{ type: 'text/javascript' }
      == window.json_data = '#{task.json_escaped}';
    .term_dates_graph

    %hr

    .term_dates_table
