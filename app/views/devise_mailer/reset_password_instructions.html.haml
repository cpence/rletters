%h1= t('users.mailer.reset.intro', name: @resource.name)
%p.lead= t('users.mailer.reset.instructions')
%p= link_to(t('users.mailer.reset.link'), edit_user_password_url(@resource, reset_password_token: @resource.reset_password_token))
%p= t('users.mailer.reset.ignore')
%p= t('users.mailer.reset.no_change')
