%h1.text-center= t('devise.mailer.reset.intro', name: @resource.name)

%p.text-center= t('devise.mailer.reset.instructions')
%p
  %button.large.expand{ href: edit_user_password_url(reset_password_token: @token) }= t('devise.mailer.reset.link')

%p= t('devise.mailer.reset.ignore')
%p= t('devise.mailer.reset.no_change')
